// Sponsor configuration. While shopping for a sponsor this stays a placeholder,
// which doubles as the pitch — it shows a prospect exactly where their brand lands
// (top strip + every share image). When one signs, set `name` (and optionally
// `url`/`logo`) and it propagates everywhere automatically.
export const SPONSOR = {
  name: null, // e.g. "Coldwell Banker Action"
  url: null, // e.g. "https://example.com"
  logo: null, // optional image URL / data URI
};
